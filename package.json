{
  "name": "legacy-booth-app",
  "version": "1.0.0",
  "description": "The Legacy Booth - A private storytelling space for seniors in assisted living facilities",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "cd backend && npm start",
    "init-db": "cd backend && node scripts/init-db.js"
  },
  "keywords": ["legacy", "seniors", "storytelling", "assisted-living"],
  "author": "Legacy Booth Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 